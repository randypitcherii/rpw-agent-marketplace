{
  "_comment": "Sample config for Glean. Merge into your MCP client config. No secrets.",
  "mcpServers": {
    "glean": {
      "command": "uv",
      "args": ["run", "--project", "${CLAUDE_PLUGIN_ROOT}/mcp-servers/glean", "python", "${CLAUDE_PLUGIN_ROOT}/mcp-servers/glean/run_mcp.py"]
    }
  }
}
