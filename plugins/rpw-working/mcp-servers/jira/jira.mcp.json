{
  "_comment": "Sample config for Jira. Merge into your MCP client config. No secrets.",
  "mcpServers": {
    "jira": {
      "command": "uv",
      "args": ["run", "--project", "${CLAUDE_PLUGIN_ROOT}/mcp-servers/jira", "python", "${CLAUDE_PLUGIN_ROOT}/mcp-servers/jira/run_mcp.py"]
    }
  }
}
