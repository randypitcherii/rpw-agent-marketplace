{
  "_comment": "Sample config for Gmail. Merge into your MCP client config. No secrets.",
  "mcpServers": {
    "google-gmail": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}/mcp-servers/google-gmail",
        "python",
        "${CLAUDE_PLUGIN_ROOT}/mcp-servers/google-gmail/run_mcp.py"
      ]
    }
  }
}
